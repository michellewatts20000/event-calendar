import masthead from 'nine-immersive-header';

masthead({
  headerConfig: {
    logoColor: '#fff',
    bgColor: '#0A1633',
    textColor: '#fff',
  },
  footerConfig: {
    bgColor: '#0A1633',
    logoColor: '#fff',
  },
});
